// Esta função captura o click do botão e exibi na tela. 
function inserir(num) {
    const numero = document.getElementById('resultado').innerHTML
    document.getElementById('resultado').innerHTML = numero + num
}